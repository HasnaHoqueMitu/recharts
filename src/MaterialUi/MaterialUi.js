import React from 'react';
import Button from '@material-ui/core/Button';

const MaterialUi = () => {
    return (
        <div>
            <h1>material</h1>
        </div>
    );
};

export default MaterialUi;